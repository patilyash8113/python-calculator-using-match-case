# python-calculator-using-match-case

# a=int(input("Enter the first number:"))
# b=int(input("Enter the second number:"))

# op=input("Enter operation(+,-,*,/):")                    # Here i ask user to enter two numbers now the user select a operations there are 4 operations(+,-,*,/).now we match operation and create 4 cases for 4                                                                 operations suppose a user enter first no,4 and second 4 and if he select + then got output 8.thats how it works....

# match op:
#     case "+":
#         print("Result:",a+b)
#     case "-":
#         print("Result:",a-b)
#     case "*":
#         print("Result:",a*b)
#     case "/":
#         if b !=0:
#             print("Result",a/b)
#         else:
#             print("cannot divide by zero")
#     case _:
#         print("Invalid operation")
